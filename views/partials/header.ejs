<!DOCTYPE html>
<html lang="en">
<head>
    <title>Gamedigger</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header class="header-class">
        <a href="/"><img class="logo-image" src="/images/Logo.png" alt="image"></a>

        <ul class="header-align">
          <% if(!user){ %>
            <li><a href="/users/login">Login</a></li>
            <li><a href="/users/new">Signup</a></li>
          <% } else { %>
            <li><a href="/connections/new">New Connection</a></li>
            <li><a href='/users/profile'>Profile</a></li>
            <li><a href='/users/logout'>Logout</a></li>
          <% } %>
        </ul>
        <hr>
        <ul>
          <li><a href="/connections">Connections</a></li>
          <% if(firstName && lastName){ %>
            <div class="li-right">
                <ul>
                    <li>Welcome <%= firstName+' '+lastName %>!</li>
                </ul>
            </div>
          <% } %>
        </ul>
    </header>
    
    <% if(errorMessages.length > 0) {%>
      <% errorMessages.forEach(message=>{%>
          <div class='error'>
              <%=message%>
          </div>
      <%});%>
    <%}%>
    
    <% if(successMessages.length > 0) {%>
        <% successMessages.forEach(message=>{%>
            <div class='success'>
                <%=message%>
            </div>
        <%});%>
    <%}%>